
Процедура Печать_Накладной(ТабДок, Ссылка) Экспорт

	Макет = Документы.ОприходованиеТоваров.ПолучитьМакет("Накладная");
	Запрос = Новый Запрос;
	Запрос.Текст =
	"ВЫБРАТЬ
	|	ОприходованиеТМЦ.Дата,
	|	ОприходованиеТМЦ.Комментарий,
	|	ОприходованиеТМЦ.Номер,
	|	ОприходованиеТМЦ.Организация,
	|	ОприходованиеТМЦ.Склад,
	|	ОприходованиеТМЦ.СуммаДокумента КАК Всего,
	|	ОприходованиеТМЦ.ИнвентаризацияТМЦ,
	|	ОприходованиеТМЦ.Товары.(
	|		НомерСтроки,
	|		Номенклатура КАК Товар,
	|		Количество,
	|		Цена,
	|		Сумма,
	|		Партия,
	|		ЕдиницаИзмерения
	|	)
	|ИЗ
	|	Документ.ОприходованиеТМЦ КАК ОприходованиеТМЦ
	|ГДЕ
	|	ОприходованиеТМЦ.Ссылка В(&Ссылка)";
	
	Запрос.УстановитьПараметр("Ссылка", Ссылка);
	Выборка = Запрос.Выполнить().Выбрать();

	ОбластьШапка       = Макет.ПолучитьОбласть("Шапка");
	ОбластьСтрока      = Макет.ПолучитьОбласть("Строка");
	ОбластьПодвал      = Макет.ПолучитьОбласть("Подвал");

	ТабДок.Очистить();
		
	Пока Выборка.Следующий() Цикл

		ОбластьШапка.Параметры.Заполнить(Выборка);
		ОбластьШапка.Параметры.ТекстЗаголовка = Ссылка; 
		ТабДок.Вывести(ОбластьШапка);

		ВыборкаТовары = Выборка.Товары.Выбрать();
		
		Пока ВыборкаТовары.Следующий() Цикл
			
			ОбластьСтрока.Параметры.Заполнить(ВыборкаТовары);
			ТабДок.Вывести(ОбластьСтрока);
			
		КонецЦикла;

		ОбластьПодвал.Параметры.Заполнить(Выборка);
		
		ОбластьПодвал.Параметры.ИтоговаяСтрока = "Всего наименований " + ВыборкаТовары.Количество()
	                                           + ", на сумму " + Выборка.Всего + " руб.";
											   
		ОбластьПодвал.Параметры.СуммаПрописью =  ЧислоПрописью(Выборка.Всего, "L=ru_RU", "рубль, рубля, рублей, м, копейка, копейки, копеек, ж, 2");

		ТабДок.Вывести(ОбластьПодвал);

	КонецЦикла;

КонецПроцедуры
